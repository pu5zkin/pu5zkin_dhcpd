---
driver:
  name: dokken
  chef_version: latest

transport:
  name: dokken

provisioner:
  name: dokken

verifier:
  name: inspec

platforms:
- name: chefdk
  driver:
    image: chef/chefdk

suites:
  - name: default
    run_list:
    - recipe[pu5zkin_dhcpd::default]